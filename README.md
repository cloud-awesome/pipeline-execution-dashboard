# pipeline-execution-dashboard

A reusable React-based dashboard component for presenting pipeline execution status across one or more repositories or systems.

The component is intentionally platform-agnostic, so can support GitHub, Azure DevOps, etc. (The query and presentation logic is abstracted away from the component and is the responsibility of the consuming application.)
